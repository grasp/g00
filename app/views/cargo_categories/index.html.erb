<h1>Listing cargo_categories</h1>

<table>
  <tr>
    <th>Code</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cargo_categories.each do |cargo_category| %>
  <tr>
    <td><%= cargo_category.code %></td>
    <td><%= cargo_category.name %></td>
    <td><%= link_to 'Show', cargo_category %></td>
    <td><%= link_to 'Edit', edit_cargo_category_path(cargo_category) %></td>
    <td><%= link_to 'Destroy', cargo_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cargo category', new_cargo_category_path %>
